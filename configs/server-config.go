package configs

const (
	// SERVER_HOSTNAME is the name of the server where the listener will run, i.e. localhost
	SERVER_HOSTNAME string = ""

	// SERVER_PORT is the port number where the server's listener will run
	SERVER_PORT string = "8081"
)
